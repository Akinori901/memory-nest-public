# API のベース URL。
# ローカル開発では未設定でよい（vite の /api プロキシが localhost:9292 に転送）。
# 本番では API Gateway の URL を設定する。
VITE_API_BASE_URL=

# Cognito（CDK デプロイ後の Output 値を設定）
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_WEB_CLIENT_ID=
VITE_COGNITO_REGION=ap-northeast-1
